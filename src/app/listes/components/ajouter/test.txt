<div class="main">



    <form #myDataFormclass="ngForm" (ngSubmit)="mySubmit(myDataFormclass)">


      <table class="example-full-width" cellspacing="0"><tr>

        <!-- Média contact -->
      <td>
          <mat-form-field class="example-full-width"  appearance="fill">
            <mat-label>Média contact</mat-label>
            <mat-select ngModel name="mediaContact" class="matSelect">
              <mat-option value="0"  disabled >Média contact</mat-option>
              <mat-option class="options" *ngFor="let MCD of mediaContactData" value="{{MCD.id}}" >{{MCD.name}}</mat-option>
            </mat-select>
          </mat-form-field>
      </td>

       <!-- Technologie -->
      <td>
        <mat-form-field class="example-full-width" appearance="fill">
          <mat-label>Technologie</mat-label>
          <mat-select ngModel name="technologie" class="matSelect">
            <mat-option disabled >Technologie </mat-option>
            <mat-option class="options" *ngFor="let MCD of mediaContactData" value="{{MCD.id}}">{{MCD.name}}</mat-option>
          </mat-select>
        </mat-form-field>
      </td>
      </tr>

     <tr>
      <!-- ID CLIENT -->
      <td>
          <mat-form-field  class="example-full-width" appearance="fill">
          <mat-label>ID CLIENT</mat-label>
          <input matInput ngModel name="idClient" #IdClient="ngModel" (change)="test(IdClient)">
        </mat-form-field>
      </td>

      <!-- Raison du contact -->
      <td>
        <mat-form-field class="example-full-width" appearance="fill">
          <mat-label>Raison du contact</mat-label>
          <mat-select ngModel name="raison" class="matSelect">
            <mat-option value="0" disabled > Raison du contact </mat-option>
            <mat-option class="options" *ngFor="let rais of raisonData" value="{{rais.id}}">{{rais.name}}</mat-option>
          </mat-select>
        </mat-form-field>
      </td>
      </tr>

      <tr>
        <!-- Motif de résiliation -->
        <td *ngIf="true">
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>Motif de résiliation</mat-label>
            <mat-select ngModel name="motif" class="matSelect">
              <mat-option value="0" disabled > Motif de résiliation </mat-option>
              <mat-option class="options" *ngFor="let motif of MotifData" value="{{motif.id}}">{{motif.name}}</mat-option>
            </mat-select>
          </mat-form-field>
        </td>

        <td *ngIf="true">
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>Retenu</mat-label>
            <mat-select ngModel name="retenu" class="matSelect">
              <mat-option value="0" disabled > Retenu </mat-option>
              <mat-option class="options" *ngFor="let retenu of retenuData" value="{{retenu.id}}">{{retenu.name}}</mat-option>
            </mat-select>
          </mat-form-field>
        </td>
      </tr>

      <tr>
        <!-- Détail Hors Cible -->
        <td *ngIf="true">
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>Détail Hors Cible</mat-label>
            <mat-select ngModel name="detail" class="matSelect">
              <mat-option value="0" disabled > Détail Hors Cible </mat-option>
              <mat-option class="options" *ngFor="let DHC of DetailHCData" value="{{DHC.id}}">{{DHC.name}}</mat-option>
            </mat-select>
          </mat-form-field>
        </td>

        <!-- Commentaire RET -->
        <td>
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>Commentaire RET</mat-label>
            <input ngModel name="Commentaire" matInput>
          </mat-form-field>
        </td>
      </tr>

      <tr>
        <!-- NDI -->
        <td>
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>NDI</mat-label>
            <input ngModel name="ndi"  matInput>
          </mat-form-field>
        </td>

        <!-- Code Postal -->
        <td>
          <mat-form-field class="example-full-width" appearance="fill">
            <mat-label>Code Postal</mat-label>
            <input ngModel name="codePostal"  matInput>
          </mat-form-field>
        </td>
      </tr>
<tr>
  <td></td>
  <td>
      <button type="submit" class="btn AddButton">AJOUTER <mat-icon class="icon-class">send</mat-icon>&nbsp;&nbsp;&nbsp;&nbsp;</button>
    </td></tr>
    </table>

  </form>
  {{ myDataFormclass.value | json }}
</div>

